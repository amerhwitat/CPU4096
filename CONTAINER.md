# Containerized build environment

```bash
docker compose build
docker compose up
```

The container provides the C/C++/CMake/Python toolchain for reproducible simulator development. Native execution remains available outside Docker.
