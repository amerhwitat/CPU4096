# Node.js implementation

Node.js ESM is the integration and reproducibility layer for CPU4096 workloads. It consumes deterministic crypto/AI vectors, benchmark metadata and C8192/R8192-compatible workload descriptions. It does not perform address-targeted private-key enumeration.