# Web Implementations

`web/javascript/` provides a browser register visualizer using JavaScript BigInt for convenient arbitrary-width integer inspection. The PHP endpoint exposes metadata only. It is not a replacement for the native CPU implementation; future WebAssembly can host a bounded simulator with explicit feature detection.