# Cross-language workloads

Python, Node.js, Java and C++ consume deterministic CPU/crypto/AI workload envelopes. Workload metadata records register width, vector shape, dataset hash, seed and implementation version so results can be compared without requiring identical host architectures.

The workload set covers fixed-width arithmetic, hashing, CNN/RNN/GRU and offline-RL experiments. Public or synthetic data only.