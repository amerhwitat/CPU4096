# Web UI CPU Integration

The repository supplies CPU/ISA data to the Chimera II Web UI integration hub (`amerhwitat/test`). WebGL visualization is presentation-only; native execution remains behind an authorized backend. Preserve ISA metadata and ABI compatibility while exposing registers, memory, execution state, and assembler/disassembler information through versioned Web UI contracts.
